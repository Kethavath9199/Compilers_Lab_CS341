
Make sure your input file (____.c) is present in the same folder along with all files.

OPen terminal from the folder and execute "./Compile.sh <input_file_name>"

If code is all perfect,then all the Intermediate Code and MIPS Assembly Code are generated and stored and the MIPS Code is simulated on SPIM simulator.

If your code has errors, suitable syntax and semantic errors will be printed to output.
